# Captures

Output folder for Game Tools screenshots on desktop/editor runs
(`PerformanceManager.CaptureScreenshot` / `ScreenCapture.CaptureScreenshot`).
On Android devices screenshots land in `persistentDataPath/captures/` instead —
this folder stays empty in source control (a `.gitkeep`-style marker only).
